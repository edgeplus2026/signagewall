<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- The prod player + R2 update channel are HTTPS. Cleartext is permitted ONLY
         for local dev hosts (emulator loopback + localhost) so `installDebug`
         against the vite dev server works; every other host stays HTTPS-only. -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
