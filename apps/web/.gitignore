# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# Payload upload fallback. Uploads go to R2 whenever the four R2_* variables are
# set; without them Payload writes here instead, and those files are useless to
# every other environment — the serverless filesystem is wiped each deploy.
/media/

# typescript
*.tsbuildinfo
